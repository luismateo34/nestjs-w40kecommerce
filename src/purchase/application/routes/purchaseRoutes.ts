export enum purchaseRoute {
  purchase = 'purchase',
}
export enum subRoutes {
  create = 'create',
  delete = 'delete',
  find = 'find',
  update = 'update',
}

export enum findRoutes {
  id = 'id',
  id_client = 'id_client',
  orders_date = 'orders_date',
  orders_month = 'orders_month',
}

export enum updateRoutes {
  envoy = 'envoy',
  order = 'order',
}
